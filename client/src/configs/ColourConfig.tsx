import { colors } from "@mui/material";

const ColourConfig = {
    sidebar: {
        bg: "#613DC1",
        colour: "#EEEEEE",
        hoverBg: "#1E293A",
        activeBg: "#1E253A"
    },
    topbar: {
        bg: "#fff",
        colour: "#000",
        buttonColour: "#613DC1"
    },
    mainBg: colors.grey[100]
};

export default ColourConfig;